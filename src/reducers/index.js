const counterInitialState = {
    name: "random"
};

const reducer = (state = counterInitialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};


export default reducer;